Alembic migrations. The initial revision is autogenerated from the ORM models.

- Apply: `alembic upgrade head`
- New revision: `alembic revision --autogenerate -m "message"`

Note: the FTS5 virtual table `passage_fts` is created by the initial migration
(and by `app.db.init_db.create_all` for dev/tests). SQLite migrations use
batch mode (`render_as_batch=True`) for ALTER support.
